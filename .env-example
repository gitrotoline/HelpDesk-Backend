# Django
SECRET_KEY=change-me-generate-with-get_random_secret_key
DEBUG=True

# Database (PostgreSQL)
DB_NAME=helpdesk
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432

# S3 — anexos (bucket privado). Upload pelo backend + proxy de download assinado.
# Em prod, deixe ACCESS_KEY/SECRET vazios e use IAM role.
AWS_STORAGE_BUCKET_NAME=
AWS_S3_REGION_NAME=sa-east-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
